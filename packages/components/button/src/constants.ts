export const baseClassName = 'n-button';

export const colorClasses = [
  `${baseClassName}-primary`,
  `${baseClassName}-secondary`,
  `${baseClassName}-danger`,
  `${baseClassName}-warning`,
  `${baseClassName}-success`,
  `${baseClassName}-info`,
  `${baseClassName}-light`,
  `${baseClassName}-dark`,
];
